parameters:
  configuration: ''
  projectFile: ''

steps:
- script: dotnet restore ${{ projectFile }} --force /property:Configuration=${{ parameters.configuration }} 
  displayName: 'Restore Packages (${{ parameters.configuration }})'